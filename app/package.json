{
  "name": "ensenzu-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "moon build --target js && vite",
    "build": "moon build --target js && vite build && node scripts/patch-worker-runtime.mjs",
    "preview": "vite preview",
    "deploy": "pnpm run build && pnpm exec wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.42.3",
    "@rabbita/vite": "^0.2.0",
    "vite": "^7.0.0",
    "wrangler": "^4.105.0"
  },
  "packageManager": "pnpm@11.5.3"
}
